 Options -MultiViews
RewriteEngine On

# If a directory or a file exists, serve it
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise, send to index.html
RewriteRule ^ index.html [QSA,L]
